[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=app --cov=pipeline --cov-report=term-missing"

[tool.coverage.run]
branch = true
source = ["app", "pipeline"]

[tool.ruff]
line-length = 100
target-version = "py311"
